{
    "version": 2,
    "buildCommand": "npm install --no-optional --legacy-peer-deps && npm install --save-dev eslint@8.57.0 && next build",
    "outputDirectory": ".next",
    "framework": "nextjs"
}
